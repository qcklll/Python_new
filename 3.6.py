from random import *


puzir_list = [3, 7, 9, 0]
print(puzir_list)

for i in range(len(puzir_list) - 1):
    for j in range(len(puzir_list) - i - 1):
        if puzir_list[j] > puzir_list[j+1]:
            temp = puzir_list[j]
            puzir_list[j] = puzir_list[j+1]
            puzir_list[j+1] = temp

print(puzir_list)
'''
Конечно! Вот все итерации пузырьковой сортировки для списка [3, 7, 9, 0]:

**Итерация 1:**
- Внешний цикл: `i = 0`
- Внутренний цикл: `j = 0`
    - Сравниваем `puzir_list[0]` (значение 3) с `puzir_list[1]` (значение 7). Так как 3 меньше 7, элементы остаются на своих местах.
- Внутренний цикл завершается.

**Итерация 2:**
- Внешний цикл: `i = 1`
- Внутренний цикл: `j = 0`
    - Сравниваем `puzir_list[0]` (значение 3) с `puzir_list[1]` (значение 7). Так как 3 меньше 7, элементы остаются на своих местах.
- Внутренний цикл завершается.

**Итерация 3:**
- Внешний цикл: `i = 2`
- Внутренний цикл: `j = 0`
    - Сравниваем `puzir_list[0]` (значение 3) с `puzir_list[1]` (значение 7). Так как 3 меньше 7, элементы остаются на своих местах.
- Внутренний цикл завершается.

**Итерация 4:**
- Внешний цикл: `i = 3`
- Внутренний цикл: `j = 0`
    - Сравниваем `puzir_list[0]` (значение 0) с `puzir_list[1]` (значение 3). Так как 0 меньше 3, элементы остаются на своих местах.
- Внутренний цикл завершается.

Итоговый отсортированный список: [0, 3, 7, 9]

Вы можете использовать этот комментарий в коде, чтобы сохранить описание каждой итерации пузырьковой сортировки. Удачи в изучении программирования!
'''
